---
name: 技能名称
description: 一句话描述该技能的功能
trigger: /触发词
version: 1.0.0
---

# 技能名称

## ⚠️ 执行声明
本技能被触发时，**必须实际执行**以下流程（使用 Read/Write/RunCommand 等工具），
**不得仅输出本文档内容而不执行实际操作**。

## 闭环要求
- 默认遵循 `.trae/documents/闭环执行矩阵.md` 的五段式闭环：预检 -> 执行 -> 诊断 -> 验证 -> 沉淀。
- 如果是高风险流程，必须补充失败处理、降级方案或回滚路径。

## 触发条件
- 用户输入 `/触发词` 时。
- 检测到需要 [描述特定场景] 时。

## 前置要求
- [可选] 需要存在哪些文件（如 `README.md`）。
- [可选] 需要处于什么状态（如已通过测试）。

## 执行步骤
1. **第一步：[动作名称]**
   - 使用 [工具名] 读取/搜索 [目标]。
   - 识别 [关键信息]。
2. **第二步：[动作名称]**
   - 使用 [工具名] 写入/更新 [文件]。
   - 确保符合 [规范]。
3. **第三步：[动作名称]**
   - 验证 [结果]。

## 输出要求
- **已完成动作**：产生了什么新文件、更新了什么内容、执行了哪些命令或诊断。
- **验证结果**：如何确认结果有效，或当前还缺哪一步验证。
- **沉淀结果**：更新了哪些记忆、文档或经验记录。
- **下一步建议**：用户接下来最适合做什么。

## 验收标准
- 输出必须能对应到当前项目上下文，避免空泛模板话术。
- 至少完成一次与本技能目标相关的验证动作。
- 如果未完全完成，必须说明阻塞点与建议路径。

## 异常处理
- 如果 [步骤X] 失败，则执行 [补救措施]。
- 明确告知用户故障原因及后续建议。
